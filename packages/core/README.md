# `@lambdamoo-mcp/core`

Shared contracts and runtime primitives for the index builder and MCP server: record types, canonical identities, dependency weighting, JSONL ingestion, hashing, and query embeddings.
